diff --git a/CMakeLists.txt b/CMakeLists.txt
index 284c77f..f8154c8 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -7,6 +7,8 @@ list(REMOVE_ITEM typo_src "src/typo_compiler.cc")
 
 add_library(rime-typo-objs OBJECT ${typo_src})
 
+target_include_directories(rime-typo-objs PRIVATE ${CMAKE_SOURCE_DIR}/librime/deps/marisa-trie/include)
+
 if(BUILD_SHARED_LIBS)
   set_target_properties(rime-typo-objs
     PROPERTIES
@@ -15,47 +17,51 @@ endif()
 
 set(plugin_name rime-typo PARENT_SCOPE)
 set(plugin_objs $<TARGET_OBJECTS:rime-typo-objs> PARENT_SCOPE)
-set(plugin_deps ${rime_library} PARENT_SCOPE)
+set(plugin_deps ${rime_library} marisa PARENT_SCOPE)
 set(plugin_modules "typo" PARENT_SCOPE)
 
-add_executable(typo_compiler src/typo_compiler.cc)
-target_link_libraries(typo_compiler marisa)
+if(NOT ANDROID)
+  add_executable(typo_compiler src/typo_compiler.cc)
+  target_link_libraries(typo_compiler marisa)
+endif()
 
 file(GLOB TYPO_TXT_FILES "${CMAKE_CURRENT_SOURCE_DIR}/data/*.txt")
 set(COMPILED_BIN_FILES "")
 
-foreach(TXT_FILE ${TYPO_TXT_FILES})
-    get_filename_component(BASENAME ${TXT_FILE} NAME_WE)
-    set(BIN_FILE "${CMAKE_CURRENT_BINARY_DIR}/${BASENAME}.bin")
-    
-    add_custom_command(
-        OUTPUT ${BIN_FILE}
-        COMMAND typo_compiler ${TXT_FILE} ${BIN_FILE}
-        DEPENDS ${TXT_FILE} typo_compiler
-    )
-    list(APPEND COMPILED_BIN_FILES ${BIN_FILE})
-endforeach()
-
-if(COMPILED_BIN_FILES)
-    add_custom_target(build_typo_data ALL DEPENDS ${COMPILED_BIN_FILES})
-endif()
+if(NOT ANDROID)
+  foreach(TXT_FILE ${TYPO_TXT_FILES})
+      get_filename_component(BASENAME ${TXT_FILE} NAME_WE)
+      set(BIN_FILE "${CMAKE_CURRENT_BINARY_DIR}/${BASENAME}.bin")
 
-include(GNUInstallDirs)
+      add_custom_command(
+          OUTPUT ${BIN_FILE}
+          COMMAND typo_compiler ${TXT_FILE} ${BIN_FILE}
+          DEPENDS ${TXT_FILE} typo_compiler
+      )
+      list(APPEND COMPILED_BIN_FILES ${BIN_FILE})
+  endforeach()
 
-if(NOT DEFINED RIME_DATA_DIR)
-    set(RIME_DATA_DIR "rime-data")
-endif()
+  if(COMPILED_BIN_FILES)
+      add_custom_target(build_typo_data ALL DEPENDS ${COMPILED_BIN_FILES})
+  endif()
 
-if(UNIX AND NOT APPLE)
-    set(FINAL_INSTALL_DEST "${CMAKE_INSTALL_DATADIR}/${RIME_DATA_DIR}/build")
-elseif(APPLE)
-    set(FINAL_INSTALL_DEST "/Library/Input Methods/Squirrel.app/Contents/SharedSupport/build")
-elseif(WIN32)
-    set(FINAL_INSTALL_DEST "$ENV{APPDATA}/Rime/build")
-endif()
+  include(GNUInstallDirs)
+
+  if(NOT DEFINED RIME_DATA_DIR)
+      set(RIME_DATA_DIR "rime-data")
+  endif()
+
+  if(UNIX AND NOT APPLE)
+      set(FINAL_INSTALL_DEST "${CMAKE_INSTALL_DATADIR}/${RIME_DATA_DIR}/build")
+  elseif(APPLE)
+      set(FINAL_INSTALL_DEST "/Library/Input Methods/Squirrel.app/Contents/SharedSupport/build")
+  elseif(WIN32)
+      set(FINAL_INSTALL_DEST "$ENV{APPDATA}/Rime/build")
+  endif()
 
-message(STATUS "[librime-typo] Compiled .bin files will be installed to: ${FINAL_INSTALL_DEST}")
+  message(STATUS "[librime-typo] Compiled .bin files will be installed to: ${FINAL_INSTALL_DEST}")
 
-if(COMPILED_BIN_FILES)
-    install(FILES ${COMPILED_BIN_FILES} DESTINATION "${FINAL_INSTALL_DEST}")
+  if(COMPILED_BIN_FILES)
+      install(FILES ${COMPILED_BIN_FILES} DESTINATION "${FINAL_INSTALL_DEST}")
+  endif()
 endif()
diff --git a/src/typo_filter.cc b/src/typo_filter.cc
index ace5f7c..daac2c5 100644
--- a/src/typo_filter.cc
+++ b/src/typo_filter.cc
@@ -249,37 +249,45 @@ void TypoFilter::LoadCorrections(Engine* engine, const std::string& input_type,
   trie_loaded_ = false;
   trie_.clear();
 
-  std::string user_dir = Service::instance().deployer().user_data_dir.string();
-  std::string shared_dir = Service::instance().deployer().shared_data_dir.string();
+  const auto& deployer = Service::instance().deployer();
+  std::string user_dir = deployer.user_data_dir.string();
+  std::string shared_dir = deployer.shared_data_dir.string();
 
   std::string user_txt = user_dir + "/" + base_name + ".txt";
   std::string user_bin = user_dir + "/build/" + base_name + ".bin";
-  std::string shared_bin = shared_dir + "/build/typo_" + input_type + ".bin";
-
-  if (IsTxtNewerThanBin(user_txt, user_bin)) {
-    LOG(INFO) << "TypoFilter: JIT Compiling user dictionary: " << user_txt;
-    std::ifstream file(user_txt);
-    if (file.is_open()) {
-      marisa::Keyset keyset;
-      std::string line;
-      while (std::getline(file, line)) {
-        if (line.empty() || line[0] == '#') continue;
-        size_t tab_pos = line.find('\t');
-        if (tab_pos != std::string::npos) {
-          std::string typo = line.substr(0, tab_pos);
-          std::string corrected = line.substr(tab_pos + 1);
-          if (!corrected.empty() && corrected.back() == '\r') corrected.pop_back();
-          if (!typo.empty() && typo.back() == '\r') typo.pop_back();
-          if (!typo.empty() && !corrected.empty()) {
-            std::string merged_key = typo + "\t" + corrected;
-            keyset.push_back(merged_key.c_str(), merged_key.length());
+  std::string shared_txt = shared_dir + "/" + base_name + ".txt";
+  std::string shared_bin = shared_dir + "/build/" + base_name + ".bin";
+
+  auto jit_compile =
+      [&](const std::string& txt_path, const std::string& bin_path,
+          const std::string& label) {
+        LOG(INFO) << "TypoFilter: JIT Compiling " << label
+                  << " dictionary: " << txt_path;
+        std::ifstream file(txt_path);
+        if (!file.is_open()) return;
+        marisa::Keyset keyset;
+        std::string line;
+        while (std::getline(file, line)) {
+          if (line.empty() || line[0] == '#') continue;
+          size_t tab_pos = line.find('\t');
+          if (tab_pos != std::string::npos) {
+            std::string typo = line.substr(0, tab_pos);
+            std::string corrected = line.substr(tab_pos + 1);
+            if (!corrected.empty() && corrected.back() == '\r')
+              corrected.pop_back();
+            if (!typo.empty() && typo.back() == '\r') typo.pop_back();
+            if (!typo.empty() && !corrected.empty()) {
+              std::string merged_key = typo + "\t" + corrected;
+              keyset.push_back(merged_key.c_str(), merged_key.length());
+            }
           }
         }
-      }
-      file.close();
+        file.close();
+
+        if (keyset.num_keys() == 0) return;
 
-      if (keyset.num_keys() > 0) {
-        std::string build_dir = user_dir + "/build";
+        std::string build_dir =
+            bin_path.substr(0, bin_path.find_last_of("/\\"));
         struct stat st;
         if (stat(build_dir.c_str(), &st) == -1) {
 #ifdef _WIN32
@@ -290,12 +298,28 @@ void TypoFilter::LoadCorrections(Engine* engine, const std::string& input_type,
         }
         marisa::Trie new_trie;
         new_trie.build(keyset);
-        new_trie.save(user_bin.c_str());
-        LOG(INFO) << "TypoFilter: Compiled successfully to: " << user_bin;
-      }
+        new_trie.save(bin_path.c_str());
+        LOG(INFO) << "TypoFilter: Compiled successfully to: " << bin_path;
+      };
+
+  // user_txt > shared_txt → user_bin
+  if (IsTxtNewerThanBin(user_txt, user_bin)) {
+    jit_compile(user_txt, user_bin, "user");
+  } else if (IsTxtNewerThanBin(shared_txt, user_bin)) {
+    jit_compile(shared_txt, user_bin, "shared");
+  }
+
+  // common_txt → common_bin
+  std::string common_dir = deployer.common_data_dir.string();
+  if (!common_dir.empty()) {
+    std::string common_txt = common_dir + "/typo/" + base_name + ".txt";
+    std::string common_bin = common_dir + "/typo/" + base_name + ".bin";
+    if (IsTxtNewerThanBin(common_txt, common_bin)) {
+      jit_compile(common_txt, common_bin, "common");
     }
   }
 
+  // load: user_bin → shared_bin → common_bin
   try {
     trie_.mmap(user_bin.c_str());
     trie_loaded_ = true;
@@ -306,7 +330,18 @@ void TypoFilter::LoadCorrections(Engine* engine, const std::string& input_type,
       trie_loaded_ = true;
       LOG(INFO) << "TypoFilter: Loaded shared binary: " << shared_bin;
     } catch (...) {
-      LOG(WARNING) << "TypoFilter: Failed to map binary for: " << base_name;
+      if (!common_dir.empty()) {
+        try {
+          std::string common_bin = common_dir + "/typo/" + base_name + ".bin";
+          trie_.mmap(common_bin.c_str());
+          trie_loaded_ = true;
+          LOG(INFO) << "TypoFilter: Loaded common binary: " << common_bin;
+        } catch (...) {
+          LOG(WARNING) << "TypoFilter: Failed to map binary for: " << base_name;
+        }
+      } else {
+        LOG(WARNING) << "TypoFilter: Failed to map binary for: " << base_name;
+      }
     }
   }
 }
